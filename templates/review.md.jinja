You are reviewing whether an implementation satisfies a single acceptance property, as part of an Open-Prompt Project Format (OPPF) review. Be strict and skeptical.

Project root: {{ project_root }}
Property to check (read it in full): {{ property_path }}

Inspect the implementation under the project root and decide whether it satisfies the property. Do not modify any files.
{% if exclude %}- Treat these paths as read-only external resources; do not create, modify or delete them:
{% for path in exclude %}    - {{ path }}
{% endfor %}{% endif %}
End your response with EXACTLY one final line, in one of these two forms, with nothing after it:
OPP_REVIEW: PASS
OPP_REVIEW: FAIL — <one-line reason>
