# Property: the greeting is exact

Running `bash hello.sh` from the project root must print exactly the line
`Hello, OPPF!` followed by a single newline, and nothing else, on standard
output. The exit status must be `0`.
